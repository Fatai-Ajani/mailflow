{
  "name": "mailflow-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:create": "wrangler d1 create mailflow",
    "d1:migrate": "wrangler d1 migrations apply mailflow --remote"
  },
  "devDependencies": {
    "wrangler": "^4.132.0"
  }
}
